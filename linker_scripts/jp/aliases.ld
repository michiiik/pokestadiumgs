/* Descriptive compatibility name aliases for the JP build. Empty for now;
 * see linker_scripts/us/aliases.ld for the pattern once JP has decompiled
 * owners worth aliasing. Kept so the generic $(VERSION) rule in the
 * Makefile has a source file to preprocess for every version. */
